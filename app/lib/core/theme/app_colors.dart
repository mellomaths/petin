import 'package:flutter/material.dart';

/// Application color palette (WCAG 2.1 AA compliant)
class AppColors {
  // Primary Colors - Warm, friendly colors for pet adoption
  static const Color primary = Color(0xFF6B9E78); // Green - nature, pets
  static const Color primaryDark = Color(0xFF4A7A5A);
  static const Color primaryLight = Color(0xFF8FB99A);

  // Secondary Colors
  static const Color secondary = Color(0xFFF4A261); // Warm orange
  static const Color secondaryDark = Color(0xFFE76F51);
  static const Color secondaryLight = Color(0xFFF9C784);

  // Accent Colors
  static const Color accent = Color(0xFF2A9D8F); // Teal
  static const Color accentLight = Color(0xFF4ECDC4);

  // Neutral Colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textDisabled = Color(0xFF9E9E9E);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);

  // Border Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderLight = Color(0xFFF0F0F0);

  // Shadow Colors
  static const Color shadow = Color(0x1A000000);
}

