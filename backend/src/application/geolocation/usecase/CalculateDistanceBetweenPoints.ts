import { Point } from "../Point";

export class CalculateDistanceBetweenPoints {
  // TODO: Implement CalculateDistanceBetweenPoints method
  execute(p1: Point, p2: Point, unit: "km" | "m" = "km"): number {
    return 0;
  }
}
